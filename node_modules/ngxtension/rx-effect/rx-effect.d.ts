import type { DestroyRef } from '@angular/core';
import { Observable, Subscription, type TapObserver } from 'rxjs';
type Effect<T> = Partial<TapObserver<T>> | ((value: T) => void);
type RxEffectOptions = {
    destroyRef: DestroyRef;
};
export declare function rxEffect<T>(source: Observable<T>, effect: Effect<T>, options?: RxEffectOptions): Subscription;
export declare function rxEffect<T>(source: Observable<T>, options?: RxEffectOptions): Subscription;
export {};
