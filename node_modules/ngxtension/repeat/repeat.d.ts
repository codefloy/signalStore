import { NgFor } from '@angular/common';
import * as i0 from "@angular/core";
/**
 * An extension of `NgFor` directive that allows consumers to iterate "x times" instead of through a list of items
 *
 * @param {number} count - a positive integer starting from 0
 *
 * @example
 *
 * ```html
 * <!-- before -->
 * <p *ngFor="let i of [0, 1, 2]">{{i}}</p>
 * <!-- after -->
 * <p *ngFor="let i; repeat: 3">{{i}}</p>
 * ```
 */
export declare class Repeat extends NgFor<number> {
    set ngForRepeat(count: number);
    static ɵfac: i0.ɵɵFactoryDeclaration<Repeat, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<Repeat, "[ngFor][ngForRepeat]", never, { "ngForRepeat": { "alias": "ngForRepeat"; "required": false; }; }, {}, never, never, true, never>;
}
