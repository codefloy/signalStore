import { Signal } from '@angular/core';
import { AbstractControl, FormControlStatus } from '@angular/forms';
import { Observable } from 'rxjs';
type FormEventData<T> = {
    value: T;
    status: FormControlStatus;
    touched: boolean;
    pristine: boolean;
    valid: boolean;
    invalid: boolean;
    pending: boolean;
    dirty: boolean;
    untouched: boolean;
};
export declare function allEventsObservable<T>(form: AbstractControl<T>): Observable<FormEventData<T>>;
export declare function allEventsObservable<T>(form: AbstractControl): Observable<FormEventData<T>>;
export declare function allEventsSignal<T>(form: AbstractControl<T>): Signal<FormEventData<T>>;
export declare function allEventsSignal<T>(form: AbstractControl): Signal<FormEventData<T>>;
export {};
