import { Tree } from '@nx/devkit';
import { ConvertToSelfClosingTagGeneratorSchema } from './schema';
export declare function convertToSelfClosingTagGenerator(tree: Tree, options: ConvertToSelfClosingTagGeneratorSchema): Promise<never>;
export declare function migrateComponentToSelfClosingTags(template: string): string;
export interface ElementToMigrate {
    tagName: string;
    start: number;
    end: number;
}
export default convertToSelfClosingTagGenerator;
