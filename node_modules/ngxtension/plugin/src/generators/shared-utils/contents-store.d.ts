import { Project } from 'ts-morph';
export declare class ContentsStore {
    private _project;
    collection: Array<{
        path: string;
        content: string;
    }>;
    withTransforms: Set<string>;
    get project(): Project;
    track(path: string, content: string): void;
}
